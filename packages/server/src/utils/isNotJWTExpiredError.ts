// import { JsonWebTokenError, TokenExpiredError } from "jsonwebtoken";

// function isExpiredError(error: JsonWebTokenError): error is TokenExpiredError {
//   return <JsonWebTokenError>error !== TokenExpiredError;
// }